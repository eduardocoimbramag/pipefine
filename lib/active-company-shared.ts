/**
 * Constantes da "empresa ativa" que podem ser importadas tanto no servidor
 * quanto no client (sem dependências server-only como cookies/Supabase).
 */

/** Nome do cookie que guarda a empresa ativa selecionada na topbar. */
export const ACTIVE_COMPANY_COOKIE = "pipefine_company";

/** Valor especial que representa "Todas as empresas". */
export const ALL_COMPANIES = "all";
